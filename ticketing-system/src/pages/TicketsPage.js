import React from 'react';
import { Typography } from '@mui/material';

const TicketsPage = () => {
  return (
    <div>
      <Typography variant="h4">Tickets</Typography>
      <Typography variant="body1">Here you can view and manage tickets.</Typography>
    </div>
  );
};

export default TicketsPage;
