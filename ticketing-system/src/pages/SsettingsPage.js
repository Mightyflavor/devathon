import React from 'react';
import { Typography } from '@mui/material';

const SettingsPage = () => {
  return (
    <div>
      <Typography variant="h4">Settings</Typography>
      <Typography variant="body1">Manage your settings here.</Typography>
    </div>
  );
};

export default SettingsPage;
